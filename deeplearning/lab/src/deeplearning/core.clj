(ns deeplearning
  (:require [clojure.repl :refer :all]
            [deeplearning.chapter1]
   ;
            )
  )

;; the file is a sequence of definitions (fns, vars) and comment blocks with exectubale code



(comment
  (clojure-version)
  
  ;
  )

(comment

  ;;;
  )