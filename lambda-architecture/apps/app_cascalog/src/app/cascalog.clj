(ns app.cascalog
  (:require [clojure.repl :refer :all]
            [cascalog.api :as cs]
            [cascalog.playground :as csp]
            [clojure.pprint :refer [print-table] :as pp]
   ;
            )
  )

(comment
  (clojure-version)
  csp/sentence
  ;
  )