(ns app.hadoop  
  (:require [clojure.repl :refer :all]
            [cascalog.api :as cs]
            ; [cascalog.playground :as csp]
            ; [com.backtype/dfs-datastores]
            ; [org.apache.hadoop/hadoop-core]
   ;
   ))


(comment
  (doc doc)
  (use 'cascalog.api)
  (use 'cascalog.playground)

  sentence
  ;
  )