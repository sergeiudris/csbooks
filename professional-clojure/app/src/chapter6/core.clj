(ns chapter6.core
  (:require [datomic.api :as d]
            [clojure.pprint :as pp]
            [clojure.java.io :as io]
            [clojure.repl :refer :all]
            [setup]
            )
  (:import datomic.Util)
  )


  (setup/go)


(comment

  (setup/go)

  
  )

